interface acciones {
    /* Las interfaces nos sirve para declarar metodos o variables y no definirlas, solo las declaramos
    y nos comprometemos a usarlas cuando la interface sea llamada */
    var año:Int
    fun correr()
    fun saltar()
    fun comer()
}